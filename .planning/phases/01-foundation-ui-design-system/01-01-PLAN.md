---
phase: 01-foundation-ui-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, style.css, app.js, .planning/verify-foundation.js]
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-05]
must_haves:
  truths:
    - "User sees Fraunces for headers and Figtree for body text"
    - "Sidebar navigation exists and reflects topic structure"
    - "Changing the URL hash updates the main content area"
  artifacts:
    - path: "index.html"
      provides: "Main SPA entry point"
      contains: "Fraunces"
    - path: "style.css"
      provides: "Design system and layout"
      contains: "--font-display"
    - path: "app.js"
      provides: "Hash routing and navigation"
      contains: "hashchange"
  key_links:
    - from: "index.html"
      to: "style.css"
      via: "link tag"
    - from: "index.html"
      to: "app.js"
      via: "script tag"
---

<objective>
Establish the visual foundation and structural core of the DSA Mastery Platform.

Purpose: To create a premium, editorial-style learning environment that is easy to host and maintain.
Output: Core SPA files (index.html, style.css, app.js) and a verification script.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ui-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-ui-design-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Core HTML Structure</name>
  <files>index.html</files>
  <action>
    Create a clean HTML5 template.
    Include Google Fonts: Fraunces (900, variable opsz) and Figtree (400, 600).
    Define a semantic layout:
    - &lt;nav id="sidebar"&gt; for topic links.
    - &lt;main id="content"&gt; for the pedagogical content.
    - &lt;header&gt; for the platform title.
    Add placeholders for the progress tracker.
  </action>
  <verify>ls index.html &amp;&amp; grep "Fraunces" index.html</verify>
  <done>index.html created with required semantic structure and font imports.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Design System &amp; Grid</name>
  <files>style.css</files>
  <action>
    Define CSS variables for:
    - Fonts: --font-display (Fraunces), --font-body (Figtree), --font-mono (JetBrains Mono).
    - Colors: Premium educational palette (soft whites, deep grays, accent indigo).
    Implement the layout:
    - Sidebar: Fixed/sticky, glassmorphism effect.
    - Content: Responsive max-width container, generous padding.
    - Typography: Apply optical sizing for Fraunces, line-height 1.6 for Figtree.
  </action>
  <verify>grep "\--font-display" style.css</verify>
  <done>style.css implements the "Premium Book" aesthetic and responsive grid.</done>
</task>

<task type="auto">
  <name>Task 3: Basic Hash Routing &amp; Navigation</name>
  <files>app.js</files>
  <action>
    Implement a minimal router:
    - Listen for 'hashchange' and 'DOMContentLoaded'.
    - Function to update 'active' class on sidebar links.
    - Function to render placeholder content based on hash.
    - Default route: #/introduction.
  </action>
  <verify>grep "hashchange" app.js</verify>
  <done>app.js handles URL changes and dynamic content injection.</done>
</task>

<task type="auto">
  <name>Task 4: Foundation Verification Script</name>
  <files>.planning/verify-foundation.js</files>
  <action>
    Create a Node.js script that:
    - Reads index.html to ensure main containers (#sidebar, #content) exist.
    - Verifies script and link tags are present.
    - Basic sanity check for CSS variables in style.css.
  </action>
  <verify>node .planning/verify-foundation.js</verify>
  <done>Verification script created and passes.</done>
</task>

</tasks>

<verification>
- [ ] index.html, style.css, app.js exist and are linked correctly.
- [ ] node .planning/verify-foundation.js passes.
- [ ] Manual: Open index.html in browser and verify typography/layout.
</verification>

<success_criteria>
- All tasks completed.
- Foundation reflects the "Premium Book" identity.
- Hash-based navigation works between placeholder routes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ui-design-system/01-01-SUMMARY.md`
</output>
